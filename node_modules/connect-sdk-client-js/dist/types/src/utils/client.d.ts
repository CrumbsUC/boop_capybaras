import type { DeviceInformation, Metadata } from '../types';
export declare const client: {
    unsupportedPaymentProductsInBrowser: Set<number>;
    getDeviceInformation(): DeviceInformation;
    getMetadata(): Metadata;
};
