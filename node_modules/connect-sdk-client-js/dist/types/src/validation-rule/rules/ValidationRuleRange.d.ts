import type { ValidationRule, ValidationRuleDefinition, RangeValidatorJSON } from '../../types';
import type { PaymentRequest } from '../../PaymentRequest';
export declare class ValidationRuleRange implements ValidationRule {
    readonly json: ValidationRuleDefinition<RangeValidatorJSON>;
    readonly type: string;
    readonly errorMessageId: string;
    readonly minValue: number;
    readonly maxValue: number;
    constructor(json: ValidationRuleDefinition<RangeValidatorJSON>);
    validate(value: string | number): boolean;
    validateValue(request: PaymentRequest, fieldId: string): boolean;
}
