import type { RegularExpressionValidatorJSON, ValidationRule, ValidationRuleDefinition } from '../../types';
import type { PaymentRequest } from '../../PaymentRequest';
export declare class ValidationRuleRegularExpression implements ValidationRule {
    readonly json: ValidationRuleDefinition<RegularExpressionValidatorJSON>;
    readonly type: string;
    readonly errorMessageId: string;
    readonly regularExpression: string;
    constructor(json: ValidationRuleDefinition<RegularExpressionValidatorJSON>);
    validate(value: string): boolean;
    validateValue(request: PaymentRequest, fieldId: string): boolean;
}
