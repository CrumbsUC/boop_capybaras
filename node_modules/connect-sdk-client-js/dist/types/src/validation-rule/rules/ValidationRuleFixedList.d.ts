import type { FixedListValidatorJSON, ValidationRule, ValidationRuleDefinition } from '../../types';
import type { PaymentRequest } from '../../PaymentRequest';
export declare class ValidationRuleFixedList implements ValidationRule {
    readonly json: ValidationRuleDefinition<FixedListValidatorJSON>;
    readonly type: string;
    readonly errorMessageId: string;
    readonly allowedValues: string[];
    constructor(json: ValidationRuleDefinition<FixedListValidatorJSON>);
    validate(value: string): boolean;
    validateValue(request: PaymentRequest, fieldId: string): boolean;
}
