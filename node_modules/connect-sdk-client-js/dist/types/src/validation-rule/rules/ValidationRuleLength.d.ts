import type { LengthValidatorJSON, ValidationRule, ValidationRuleDefinition } from '../../types';
import type { PaymentRequest } from '../../PaymentRequest';
export declare class ValidationRuleLength implements ValidationRule {
    readonly json: ValidationRuleDefinition<LengthValidatorJSON>;
    readonly type: string;
    readonly errorMessageId: string;
    readonly minLength: number;
    readonly maxLength: number;
    constructor(json: ValidationRuleDefinition<LengthValidatorJSON>);
    validate(value: string): boolean;
    validateValue(request: PaymentRequest, fieldId: string): boolean;
}
