import type { EmptyValidatorJSON, ValidationRule, ValidationRuleDefinition } from '../../types';
import type { PaymentRequest } from '../../PaymentRequest';
export declare class ValidationRuleLuhn implements ValidationRule {
    readonly json: ValidationRuleDefinition<EmptyValidatorJSON>;
    readonly type: string;
    readonly errorMessageId: string;
    constructor(json: ValidationRuleDefinition<EmptyValidatorJSON>);
    validate(value: string): boolean;
    validateValue(request: PaymentRequest, fieldId: string): boolean;
}
